#include
#include
using namespace std;

int main()
{
string str;
string tok = "arsf3xqiyc86ARSFXQIYC";
getline(cin, str);
for(int i=str.length(); i>=0;i--){
if(tok.find(str[i]) == std::string::npos){
cout<<str[i];
}
}

return 0;  
}
